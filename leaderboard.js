
const regionSelect=document.querySelector('#region-select'),limitSelect=document.querySelector('#limit-select'),refreshBtn=document.querySelector('#refresh-btn'),stateEl=document.querySelector('#state'),table=document.querySelector('#leaderboard-table'),thead=document.querySelector('#leaderboard-head'),tbody=document.querySelector('#leaderboard-body');
async function loadLeaderboard(){stateEl.className='state-msg';stateEl.textContent='Chargement du classement…';table.style.display='none';refreshBtn.disabled=true;try{const q=new URLSearchParams({country:regionSelect.value,limit:limitSelect.value});const r=await fetch(`${API_BASE}/leaderboard?${q}`,{cache:'no-store'});const d=await safeJson(r);if(!r.ok)throw new Error(d?.detail||d?.error||`Erreur HTTP ${r.status}`);const items=Array.isArray(d?.items)?d.items:[];if(!items.length)throw new Error('Le classement est vide pour cette sélection.');render(items);table.style.display='table';stateEl.className='state-msg state-msg--success';stateEl.textContent=`${items.length} joueurs chargés.`}catch(e){stateEl.className='state-msg state-msg--error';stateEl.textContent=e.message||'Impossible de charger le classement.'}finally{refreshBtn.disabled=false}}
function render(items){thead.innerHTML='<tr><th>#</th><th>Joueur</th><th>Tag</th><th>Clan</th><th>Ligue</th><th>Rating</th></tr>';tbody.innerHTML=items.map(p=>`<tr><td class="rank">#${esc(p.rank??'—')}</td><td><a class="player-link" href="tracker.html?tag=${encodeURIComponent(String(p.tag||'').replace('#',''))}">${esc(p.name||'—')}</a></td><td>${esc(p.tag||'—')}</td><td>${esc(p.clan?.name||'—')}</td><td>${p.leagueNumber!=null?`Ligue ${esc(p.leagueNumber)}`:'—'}</td><td class="rating">${fmt(p.eloRating??p.trophies)}</td></tr>`).join('')}
function safeJson(r){return r.json().catch(()=>null)}function fmt(v){return v==null||Number.isNaN(Number(v))?'—':new Intl.NumberFormat('fr-FR').format(Number(v))}function esc(v){return String(v??'').replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll('"','&quot;').replaceAll("'",'&#039;')}
regionSelect.addEventListener('change',loadLeaderboard);limitSelect.addEventListener('change',loadLeaderboard);refreshBtn.addEventListener('click',loadLeaderboard);loadLeaderboard();
